[pytest]
asyncio_mode = auto
markers =
    db: marks tests as requiring the database
filterwarnings =
    # Internal deprecations from testcontainers — not our code, not actionable
    ignore::DeprecationWarning:testcontainers.*
    # passlib uses the `crypt` stdlib module deprecated in Python 3.12+
    ignore::DeprecationWarning:passlib.*
